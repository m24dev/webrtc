import './OperatorApp.css.proxy.js';
/* src\js\OperatorApp.svelte generated by Svelte v3.29.6 */
import {
	SvelteComponent,
	action_destroyer,
	add_render_callback,
	append,
	attr,
	check_outros,
	create_bidirectional_transition,
	detach,
	element,
	empty,
	group_outros,
	init,
	insert,
	is_function,
	listen,
	noop,
	safe_not_equal,
	set_data,
	space,
	text,
	transition_in,
	transition_out
} from "../../web_modules/svelte/internal.js";

import { fly } from "../../web_modules/svelte/transition.js";
import Peer from "../../web_modules/peerjs.js";
import settings from "./settings.js";

function create_else_block_1(ctx) {
	let div;
	let button;
	let t;
	let mounted;
	let dispose;
	let if_block = /*isLoading*/ ctx[0] && create_if_block_3(ctx);

	return {
		c() {
			div = element("div");
			button = element("button");
			if (if_block) if_block.c();
			t = text("\r\n            Начать");
			attr(button, "type", "button");
			attr(button, "class", "btn btn-primary shadow-sm");
			button.disabled = /*isLoading*/ ctx[0];
			attr(div, "class", "popup");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, button);
			if (if_block) if_block.m(button, null);
			append(button, t);

			if (!mounted) {
				dispose = listen(button, "click", /*makeConnection*/ ctx[6]);
				mounted = true;
			}
		},
		p(ctx, dirty) {
			if (/*isLoading*/ ctx[0]) {
				if (if_block) {
					
				} else {
					if_block = create_if_block_3(ctx);
					if_block.c();
					if_block.m(button, t);
				}
			} else if (if_block) {
				if_block.d(1);
				if_block = null;
			}

			if (dirty & /*isLoading*/ 1) {
				button.disabled = /*isLoading*/ ctx[0];
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
			if (if_block) if_block.d();
			mounted = false;
			dispose();
		}
	};
}

// (109:0) {#if isPeerReady}
function create_if_block(ctx) {
	let current_block_type_index;
	let if_block;
	let if_block_anchor;
	let current;
	const if_block_creators = [create_if_block_1, create_else_block];
	const if_blocks = [];

	function select_block_type_1(ctx, dirty) {
		if (/*isRemoteStreamReady*/ ctx[2]) return 0;
		return 1;
	}

	current_block_type_index = select_block_type_1(ctx, -1);
	if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);

	return {
		c() {
			if_block.c();
			if_block_anchor = empty();
		},
		m(target, anchor) {
			if_blocks[current_block_type_index].m(target, anchor);
			insert(target, if_block_anchor, anchor);
			current = true;
		},
		p(ctx, dirty) {
			let previous_block_index = current_block_type_index;
			current_block_type_index = select_block_type_1(ctx, dirty);

			if (current_block_type_index === previous_block_index) {
				if_blocks[current_block_type_index].p(ctx, dirty);
			} else {
				group_outros();

				transition_out(if_blocks[previous_block_index], 1, 1, () => {
					if_blocks[previous_block_index] = null;
				});

				check_outros();
				if_block = if_blocks[current_block_type_index];

				if (!if_block) {
					if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);
					if_block.c();
				} else {
					if_block.p(ctx, dirty);
				}

				transition_in(if_block, 1);
				if_block.m(if_block_anchor.parentNode, if_block_anchor);
			}
		},
		i(local) {
			if (current) return;
			transition_in(if_block);
			current = true;
		},
		o(local) {
			transition_out(if_block);
			current = false;
		},
		d(detaching) {
			if_blocks[current_block_type_index].d(detaching);
			if (detaching) detach(if_block_anchor);
		}
	};
}

// (129:12) {#if isLoading}
function create_if_block_3(ctx) {
	let span;

	return {
		c() {
			span = element("span");
			attr(span, "class", "spinner-border spinner-border-sm");
			attr(span, "role", "status");
			attr(span, "aria-hidden", "true");
		},
		m(target, anchor) {
			insert(target, span, anchor);
		},
		d(detaching) {
			if (detaching) detach(span);
		}
	};
}

// (121:4) {:else}
function create_else_block(ctx) {
	let div;

	return {
		c() {
			div = element("div");
			div.innerHTML = `<p>Ожидание соединения</p>`;
			attr(div, "class", "popup");
		},
		m(target, anchor) {
			insert(target, div, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
		}
	};
}

// (110:4) {#if isRemoteStreamReady}
function create_if_block_1(ctx) {
	let div;
	let video_1;
	let video_action;
	let t;
	let if_block_anchor;
	let current;
	let mounted;
	let dispose;
	let if_block = /*isAnswered*/ ctx[3] && create_if_block_2(ctx);

	return {
		c() {
			div = element("div");
			video_1 = element("video");
			t = space();
			if (if_block) if_block.c();
			if_block_anchor = empty();
			video_1.muted = true;
			attr(div, "class", "video");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, video_1);
			insert(target, t, anchor);
			if (if_block) if_block.m(target, anchor);
			insert(target, if_block_anchor, anchor);
			current = true;

			if (!mounted) {
				dispose = action_destroyer(video_action = /*video*/ ctx[7].call(null, video_1, /*remoteStream*/ ctx[4]));
				mounted = true;
			}
		},
		p(ctx, dirty) {
			if (video_action && is_function(video_action.update) && dirty & /*remoteStream*/ 16) video_action.update.call(null, /*remoteStream*/ ctx[4]);

			if (/*isAnswered*/ ctx[3]) {
				if (if_block) {
					if_block.p(ctx, dirty);

					if (dirty & /*isAnswered*/ 8) {
						transition_in(if_block, 1);
					}
				} else {
					if_block = create_if_block_2(ctx);
					if_block.c();
					transition_in(if_block, 1);
					if_block.m(if_block_anchor.parentNode, if_block_anchor);
				}
			} else if (if_block) {
				group_outros();

				transition_out(if_block, 1, 1, () => {
					if_block = null;
				});

				check_outros();
			}
		},
		i(local) {
			if (current) return;
			transition_in(if_block);
			current = true;
		},
		o(local) {
			transition_out(if_block);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div);
			if (detaching) detach(t);
			if (if_block) if_block.d(detaching);
			if (detaching) detach(if_block_anchor);
			mounted = false;
			dispose();
		}
	};
}

// (114:8) {#if isAnswered}
function create_if_block_2(ctx) {
	let div2;
	let div1;
	let div0;
	let t;
	let div2_transition;
	let current;

	return {
		c() {
			div2 = element("div");
			div1 = element("div");
			div0 = element("div");
			t = text(/*answer*/ ctx[5]);
			attr(div0, "class", "answer__inner rounded bg-primary d-flex justify-content-center align-items-center svelte-5ojqld");
			attr(div1, "class", "p-4 d-flex justify-content-center");
			attr(div2, "class", "answer svelte-5ojqld");
		},
		m(target, anchor) {
			insert(target, div2, anchor);
			append(div2, div1);
			append(div1, div0);
			append(div0, t);
			current = true;
		},
		p(ctx, dirty) {
			if (!current || dirty & /*answer*/ 32) set_data(t, /*answer*/ ctx[5]);
		},
		i(local) {
			if (current) return;

			add_render_callback(() => {
				if (!div2_transition) div2_transition = create_bidirectional_transition(div2, fly, { y: 100, opacity: 0 }, true);
				div2_transition.run(1);
			});

			current = true;
		},
		o(local) {
			if (!div2_transition) div2_transition = create_bidirectional_transition(div2, fly, { y: 100, opacity: 0 }, false);
			div2_transition.run(0);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div2);
			if (detaching && div2_transition) div2_transition.end();
		}
	};
}

function create_fragment(ctx) {
	let current_block_type_index;
	let if_block;
	let if_block_anchor;
	let current;
	const if_block_creators = [create_if_block, create_else_block_1];
	const if_blocks = [];

	function select_block_type(ctx, dirty) {
		if (/*isPeerReady*/ ctx[1]) return 0;
		return 1;
	}

	current_block_type_index = select_block_type(ctx, -1);
	if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);

	return {
		c() {
			if_block.c();
			if_block_anchor = empty();
		},
		m(target, anchor) {
			if_blocks[current_block_type_index].m(target, anchor);
			insert(target, if_block_anchor, anchor);
			current = true;
		},
		p(ctx, [dirty]) {
			let previous_block_index = current_block_type_index;
			current_block_type_index = select_block_type(ctx, dirty);

			if (current_block_type_index === previous_block_index) {
				if_blocks[current_block_type_index].p(ctx, dirty);
			} else {
				group_outros();

				transition_out(if_blocks[previous_block_index], 1, 1, () => {
					if_blocks[previous_block_index] = null;
				});

				check_outros();
				if_block = if_blocks[current_block_type_index];

				if (!if_block) {
					if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);
					if_block.c();
				} else {
					if_block.p(ctx, dirty);
				}

				transition_in(if_block, 1);
				if_block.m(if_block_anchor.parentNode, if_block_anchor);
			}
		},
		i(local) {
			if (current) return;
			transition_in(if_block);
			current = true;
		},
		o(local) {
			transition_out(if_block);
			current = false;
		},
		d(detaching) {
			if_blocks[current_block_type_index].d(detaching);
			if (detaching) detach(if_block_anchor);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	const query = new URLSearchParams(location.search);
	const id = query.get("id");
	let peer;
	let isLoading = false;
	let isPeerReady = false;
	let isRemoteStreamReady = false;
	let isAnswered = false;
	let dataConnection;
	let mediaConnection;
	let remoteStream;
	let answer = "";

	function makeConnection() {
		$$invalidate(0, isLoading = true);
		peer = new Peer(`operator${id}`, settings.callOptions);

		peer.on("open", function () {
			console.log("open");
			$$invalidate(1, isPeerReady = true);
		});

		peer.on("connection", function (conn) {
			console.log("connection");

			if (dataConnection) {
				dataConnection.close();
			}

			dataConnection = conn;
			dataConnection.on("data", handleData);

			dataConnection.on("close", () => {
				
			});

			dataConnection.on("error", err => {
				console.log(err);
			});
		});

		peer.on("call", function (conn) {
			console.log("call");

			if (mediaConnection && mediaConnection.open) {
				mediaConnection.close();
			}

			mediaConnection = conn;

			mediaConnection.on("stream", stream => {
				if (remoteStream) return false;
				console.log("stream");
				$$invalidate(4, remoteStream = stream);
				$$invalidate(2, isRemoteStreamReady = true);
			});

			mediaConnection.on("error", err => {
				console.error(err);
			});

			mediaConnection.answer();
		});

		peer.on("error", err => {
			console.error(err);
		});

		peer.on("disconnected", function () {
			console.log("disconnected");
		});
	}

	function video(node, remoteStream) {
		play(node);

		return {
			update(remoteStream) {
				play(node);
			}
		};
	}

	function play(el) {
		el.srcObject = remoteStream;

		el.onloadedmetadata = () => {
			el.play();
		};
	}

	function handleData(data) {
		$$invalidate(5, answer = data.answer);
		$$invalidate(3, isAnswered = true);

		setTimeout(
			() => {
				$$invalidate(3, isAnswered = false);
			},
			3000
		);
	}

	return [
		isLoading,
		isPeerReady,
		isRemoteStreamReady,
		isAnswered,
		remoteStream,
		answer,
		makeConnection,
		video
	];
}

class OperatorApp extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default OperatorApp;